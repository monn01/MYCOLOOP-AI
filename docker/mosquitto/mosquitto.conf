# Konfigurasi Mosquitto untuk dev lokal (lihat PRD.md §6.2, TASKPLAN.md Phase 6a).
# Anonymous + tanpa TLS karena cuma jalan di localhost buat development —
# broker produksi (HiveMQ Cloud / Mosquitto self-hosted) pakai auth+TLS,
# dikonfigurasi terpisah saat deploy, bukan lewat file ini.
listener 1883
allow_anonymous true
